# 本文件【可选】。docker-compose.yml 已内联 JWT_SECRET / CMS_DATA 的默认值，
# 不准备 .env 也能直接 `docker compose up -d --build` 跑起来。
# 注意：.env 已被 .gitignore 忽略，不会进仓库；本示例文件可安全提交。
#
# 仅当你想“覆盖”默认值（例如换更强的 JWT 密钥、改用威联通/Unraid 路径）时：
#   1. cp .env.example .env
#   2. 取消下面相应行的注释并填入实际值；注释掉（或未设）则走 compose 内联默认值。
#
# JWT 密钥：用 `openssl rand -base64 48` 生成强随机值（不取消注释则使用 compose 内联默认值）
# JWT_SECRET=你的强随机值
#
# 数据目录：按你的 NAS 修改（不取消注释则使用 compose 内联的 /volume1/docker/cms/data）
# CMS_DATA=/volume1/docker/cms/data
