# 本文件【部分必填】。docker-compose.yml 已内联 JWT_SECRET / CMS_DATA 的默认值，
# 但 GITHUB_TOKEN 必须填（用于构建时从私有仓库克隆源码）。
# 注意：.env 已被 .gitignore 忽略，不会进仓库；本示例文件可安全提交。
#
# 步骤：cp .env.example .env，然后按需编辑下面的值。
#
# —— 必填：GitHub 访问令牌（repo 权限的 Personal Access Token）——
# 构建时 Docker 用它从私有仓库克隆源码。若你把 GitHub 仓库设为 Public，
# 则此项可留空，并把 docker-compose.yml 里 context 的 `x-access-token:${GITHUB_TOKEN}@` 删掉。
GITHUB_TOKEN=
#
# —— 可选：覆盖内联默认值 ——
# JWT 密钥：用 `openssl rand -base64 48` 生成强随机值（不填则用 compose 内联默认值）
# JWT_SECRET=你的强随机值
#
# 数据目录：按你的 NAS 修改（不填则用 compose 内联的 /volume1/docker/cms/data）
# CMS_DATA=/volume1/docker/cms/data
