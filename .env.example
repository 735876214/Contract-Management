# 本文件【全部可选】。docker-compose.yml 已内联 JWT_SECRET / CMS_DATA 默认值，
# 且 ghcr.io 镜像已设为 Public，部署【无需 GITHUB_TOKEN】、无需源码、无需本地构建。
# 想覆盖默认值时在 .env 设对应变量即可；不填也能直接 `docker compose up -d` 跑起来。
#
# 步骤：cp .env.example .env （可改可不改）

# JWT 密钥：用 `openssl rand -base64 48` 生成强随机值覆盖内联默认（推荐，更安全）
# JWT_SECRET=你的强随机值

# 数据目录：按你的 NAS 修改（不填则用 /volume1/docker/cms/data）
# CMS_DATA=/volume1/docker/cms/data

# 前端对外端口：默认 9080（避免与 NAS 已占用的 80 / 8080 冲突）。
# 若 9080 也被占用，改成其它端口，例如：
# CMS_HTTP_PORT=9221

